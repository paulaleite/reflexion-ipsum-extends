import XCTest
@testable import reflexion_ipsum_extends

final class reflexion_ipsum_extendsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(reflexion_ipsum_extends().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
